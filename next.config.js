/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['abuntra-innovations.vercel.app', 'media.gettyimages.com'],
  },
}

module.exports = nextConfig 